<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<title>DCS (Gitea) API</title>
		<link href="{{AssetUrlPrefix}}/css/swagger.css?v={{AssetVersion}}" rel="stylesheet">
	</head>
	<body>
		<!-- DCS Customizations -->
		<a class="swagger-back-link" href="{{AppUrl}}">{{svg "octicon-reply"}}Return to DCS</a>
		<a class="swagger-back-link" href="catalog/swagger">{{svg "octicon-tag"}}Goto <strong>Catalog Next</strong> API</a>
		<!-- END DCS Customizations -->
		<div id="swagger-ui" data-source="{{AppUrl}}swagger.{{.APIJSONVersion}}.json"></div>
		<script src="{{AssetUrlPrefix}}/js/swagger.js?v={{AssetVersion}}"></script>
	</body>
</html>
